import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

import styles from './likesAndComments.module.css'
import utilStyles from '../styles/utils.module.css'

export default function LikesAndComments({ children }) {

    return (
        <></>
    )
}